# Rust
target/
*.rs.bk
*.profraw

# Node
node_modules/
dist/
build/
*.log
.env
.env.*
!.env.example

# Tauri
examples/tauri-app/src-tauri/gen/

# OS / editors
.DS_Store
Thumbs.db
*.tmp
*.swp
.idea/
.vscode/
react/dist/
react/node_modules/
supabase/

# Local npm tarballs from `make pack`
dist-packages/

# Agent skill mirrors — canonical copies live in docs/skills/
.agents/
.grok/

# Apple sign-in signing keys. The .p8 mints the GoTrue client secret, and Apple
# caps that JWT at six months — so these rotate, and each new key arrives under
# a different AuthKey_<id>.p8 name. Match the extension, not one filename.
*.p8

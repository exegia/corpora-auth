{
  "name": "corpora-auth-workspace",
  "private": true,
  "workspaces": [
    "guest-js",
    "ui",
    "examples/tauri-app"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "test:e2e": "cargo test --test e2e_lifecycle -- --ignored --nocapture"
  },
  "packageManager": "bun@1.3.2"
}
